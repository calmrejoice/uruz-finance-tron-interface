import { Flex, Text } from "@chakra-ui/react";

export const ProposalVoteModal = ({ proposalId, isOpen, onClose }: any) => {
  return (
    <Flex>
      <Text>ProposalVoteModal</Text>
    </Flex>
  );
};
