// import { config } from "@constants/config";
// import { formatBalance } from "@utils/formatBalance";

export const useMarketDetails = async (tronWeb: any, utokenAddress: string) => {
  //   const contract = await tronWeb.contract().at(utokenAddress);
  //   const totalSupplyBN = await contract.totalSupply().call();
  //   const apy = 10;
  //   const totalBorrowBN = await contract.totalBorrows().call();
  //   const borrowApy = 20;
  //   const availableLending = await contract.getCash().call();
  //   return {
  //     totalSupply: formatBalance(totalSupplyBN, config.utokenDecimals),
  //     apy,
  //     totalBorrow: formatBalance(totalBorrowBN, config.utokenDecimals),
  //     borrowApy,
  //     availableLending,
  //   };
};
